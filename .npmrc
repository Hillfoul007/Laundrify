# Memory optimization for npm install
fund=false
audit=false
progress=false
# Reduce parallel downloads to conserve memory
maxsockets=1
# Use faster registry
registry=https://registry.npmjs.org/
# Disable optional dependencies to save space
optional=false
# Disable dev dependencies in production
production=true
